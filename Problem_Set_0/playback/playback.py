# Prompt the user to enter some text. The input() function returns a string.
play = input("Enter text: ").replace(" ", "...")

# The .replace(" ", "...") part replaces every space in the input with three dots.

# Print the modified string to the screen.
print(play)